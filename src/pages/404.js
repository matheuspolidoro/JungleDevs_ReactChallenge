export default function NotFound() {
  return (
    <section
      style={{
        display: "flex",
        alignItems: "center",
        width: "100%",
        height: "100%",
        flexDirection: "column",
      }}
    >
      <h3
        style={{
          flex: 1,
        }}
      >
        Page not found.
      </h3>
    </section>
  );
}
